spring:
  datasource:
    url: jdbc:h2:mem:drift-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

drift:
  etl:
    on-startup: false
